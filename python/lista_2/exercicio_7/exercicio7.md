Você está desenvolvendo um sistema de análise de dados financeiros e precisa criar um programa em Python para realizar operações avançadas em um **conjunto de dados** representado por uma lista de **números inteiros** (poderia ser a quantidade de ações compradas de uma determinada empresa). 

Sua tarefa é criar um programa Python que faça o seguinte: 
> leia esses dados e armazene em uma List de números inteiros (dica: usar o método append da uma List), montando assim um vetor de números inteiros. 
> Salvar esse vetor em um arquivo, para utilizar nas próximas etapas. 

Crie um programa ou nova funcionalidade no programa anterior (pode ser um novo bloco de um google colab, ou outro programa no mesmo projeto) que leia o arquivo gerado no exercício anterior e armazenar em um vetor de números inteiros (array ou List de inteiros). O programa deve calcular sem usar funções prontas e imprimir: o **valor médio, o valor máximo, o valor mínimo, os 5 primeiros valores e os últimos 5 valores**. Se houver algum valor menor ou igual a zero, que é um valor impossível, o seu programa deve substituir esse valor pela média calculada (portanto, os valores faltantes ou inválidos não devem ser utilizados nos cálculos de média, mínimo, máximo). Armazenar em outro arquivo o vetor novo sem valores inválidos.

Crie um programa que leia o arquivo gerado no exercício anterior e armazenar em um vetor de números inteiros (array ou List de inteiros). O programa deve calcular (agora usando chamadas de funções prontas em bibliotecas) e imprimir: o **valor médio, mediana, o valor máximo, o valor mínimo, valor da amplitude, os 5 primeiros valores e os últimos 5 valores, desvio padrão e variância**. Explicar sucintamente cada um desses dos valores (em 2 linhas no máximo para cada valor, colocar isso em comentários para esclarecer ao usuário que usar seu programa). 

Utilize o mesmo programa anterior, implementando mais uma funcionalidade. O programa agora deve gerar um novo (outro arquivo de saída), mas deve eliminar os “outliers”. Mas o que são “outliers”? Eles são “pontos fora da curva”, são valores extremamente grandes ou extremamente pequenos, que pode atrapalhar a sua análise. Um valor maior que a média mais 2 vezes o desvio padrão é outlier. Ou um valor menor que a média menos 2 vezes o desvio padrão também é outlier. O seu programa deve fazer este processo sem usar funções prontas. Imprima o vetor resultado. Provavelmente, agora você vai preferir trocar os valores do arquivo que está sendo trabalhado (verifique se é possível editar o arquivo gerado anteriormente).